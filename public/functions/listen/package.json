{
  "scripts": {
    "deploy": "gcloud alpha functions deploy listen --stage-bucket primo-functions --trigger-http --include-ignored-files"
  },
  "dependencies": {
    "@google-cloud/datastore": "^0.6.0",
    "@slack/client": "^3.8.1"
  }
}
